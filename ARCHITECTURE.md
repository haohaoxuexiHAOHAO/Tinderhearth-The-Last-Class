# 架构

本文写**分层、边界与为什么**。产品事实在设计仓的正典，跨域取舍在设计仓 `decisions/` 的 ADR，
本文只说代码这一侧的结构。与正典冲突时以正典为准。

## 两层：规则层与引擎层

```
tests/  Tinderhearth.Rules.Tests   ← 只测规则层
   ↓
rules/  Tinderhearth.Rules         ← 判定与结算规则，不引用 Godot
   ↑
src/    Tinderhearth-The-Last-Class ← 场景、节点、输入、显示、文件 I/O
```

依赖是**单向**的：引擎层依赖规则层，规则层对引擎层一无所知。

### 为什么规则层必须是独立程序集，而不是一条约定

实测：在引擎外构造 Godot 节点（`new Godot.Node()`）会触发**进程级 Fatal error**，崩在
`Node..cctor()`，**而且 try/catch 抓不住**。也就是说一次手滑不会变成一条失败的测试，而是
带走整轮测试的全部结果，还看不出是哪一行干的。

规则层不引用 GodotSharp，编译器就不给手滑的机会 —— 这是靠构造保证，不是靠自觉。

顺带一条实测结论，避免以后有人拿它当反例：Godot 的**纯数学结构体**（`Vector2I` 之类）在引擎外
是可用的，因为它们是纯托管类型。所以「让规则层引用 GodotSharp 换取方便」技术上行得通，但那会
把 `Node` 的门一起打开，护栏就软了。**要开这个口子，先在本文写明开的是哪个口子、为什么。**

### 这条边界同时是别的东西的地基

- 规则层不碰引擎，所以它的测试不需要启动 Godot：跑得快，而且「能不能跑测试」不依赖「引擎能不能加载程序集」。
- 联机若做，规则层是唯一需要在两端得出相同结果的东西（设计仓 `canon/gameplay/玩法定位.md` 写明联机未立项）。
- mod 只能追加内容、不能改规则 —— 规则是编译进程序集的代码，不在数据文件里。

## 模块边界照着正典的系统清单来

设计仓 `canon/gameplay/玩法定位.md` 的「系统清单」是**分层的**（五个大系统各含子系统），
那份层级就是天然的模块边界。命名空间照它排：

| 正典里的大系统 | 命名空间 |
| --- | --- |
| 跨系统基础设施 | `Tinderhearth.Rules.Foundation` |
| 时间与经营系统 | `Tinderhearth.Rules.Economy` |
| 角色与成长系统 | `Tinderhearth.Rules.Progression` |
| 战斗与关卡系统 | `Tinderhearth.Rules.Combat` |
| 叙事与推进系统 | `Tinderhearth.Rules.Narrative` |

**这张表是「东西该放哪」的权威，文件系统不是。** 目录只在有代码时才建 —— 现在只有
`Foundation/` 与 `Progression/`，因为只有它们有代码。建一堆空目录是把没做的事伪装成做了的事。

子系统清单**不在本文重抄一遍**：正典是它的唯一权威源，抄一份必然漂移。要知道某个子系统属于哪个
大系统、第一版做不做，去查正典那三份分册。

## 内容与 mod 的加载

**基础内容 `res://data`，mod `user://mods/<名字>/`，后者叠在前者之上。**

先纠正一条本文自己曾经写错的说法：**「导出后 `res://` 只读」是错的。** 2026-08-30 用导出产物实测
（Windows、非嵌入 pck）：往 `res://` 写文件**会成功**，句柄非 null、错误码 `Ok`，文件落在**可执行
文件同级目录**。只有已经打进 `.pck` 的条目才真正改不了，`res://` 这个前缀本身是可写的。

所以 mod 放 `user://` 的理由不是「res:// 写不了」，而是这三条：

1. **exe 同级目录不按用户区分。** 多用户共享一份安装时，一个人装的 mod 会影响所有人。
2. **真实安装位置常常写不了或会被抹掉。** `C:\Program Files` 下要提权；Steam 库目录即使可写，
   「验证文件完整性」也会清掉不在清单里的文件 —— 玩家的 mod 会莫名消失。
3. **Steam 云存档要求一个可预测的按用户位置**，`user://` 正是它（`ENG-8`）。

已实测：`user://` 可创建可读写；放一个探针 mod 进去后它被发现、内容被叠加、基础内容被覆盖，
验证完已删除。

### 两种覆盖语义，别统一成一种

| 内容形状 | 语义 | 例子 |
| --- | --- | --- |
| 一个文件一个实体 | **整文件覆盖**，后来的来源整份替换 | 角色、宝物、配方、敌人 |
| 一个文件很多条目 | **逐条目合并**，只覆盖同键条目 | 文本表 |

这个区分是实测撞出来的，不是设计时想到的：最初文本也走整文件覆盖，结果一个只提供三条文本的 mod
把基础文本从 5 条顶成 3 条，`boot.title` 直接消失 —— 界面上会冒出一片缺文本占位。改成逐键合并后
同一场景是 6 条（基础 5 条 + mod 新增 1 条，2 条被覆盖）。

对应代码：`ContentCatalog.Resolve` 是整文件覆盖，`ContentCatalog.ResolveAll` 不去重、由调用方
决定合并方式；文本走 `TextCatalog.Merge`。两条语义都有测试盯着。

### 每条内容记得来自哪个来源

`ContentEntry` 带 `SourceName`。这不是为了诊断好看：待办 `ENG-7` 要求卸载 mod 后能列出「因缺少 mod
而不可用」的角色与物品，那件事只能靠来源信息做，事后补要翻遍整条加载路径。

## `ENG-5` 的四条零成本预留在哪

这四条的共同点是**失效方式都静默**：图省事写死一个数字或一句中文，代码照样跑、游戏照样玩，
等到做 mod 或联机才发现要翻遍代码。所以每条都有测试盯着（`tests/Foundation/Eng5ReservationTests.cs`）。

| 预留 | 落点 |
| --- | --- |
| 角色定义外置成数据 | `Progression/CharacterDefinition.cs` + `data/characters/*.json` |
| 名册容量从配置读 | `Foundation/Config/GameConfig.cs` + `data/config/game.json` |
| 本地玩家／AI／远程玩家统一成控制器接口 | `Foundation/Actors/ActorControl.cs` |
| 全部文本外置 | `Foundation/Text/TextCatalog.cs` + `data/text/*.json` |

关于控制器接口，有一条容易被写坏、所以单独写明：**「玩家」不是「主角」的同义词。** 主角只是恰好
通常由 `LocalPlayer` 驱动的一个角色。谁驱动谁由 `ActorControllerRegistry` 决定，任何
「这个角色是不是主角」的判断都不得用来决定它由谁驱动，反之亦然。有一条测试直接把主角换成 AI 驱动
来盯这件事。

## 刻意还不存在的东西

写下来是为了让「还没做」和「忘了做」区分得开：

- **任何玩法数值。** 属性公式、成长曲线、判定公式、价格、消耗量、时长归 `GP-2`，尚未设计。
  代码与数据里都**刻意没有**这类数字 —— 现在写进去就是把猜出来的数伪装成配置。
- **逻辑分辨率与界面结构**（`UI-1`）。所以没有任何界面代码。
- **验收总入口**（`ENG-3`）。构建、测试与导出现在各是一条命令，串联、日志落盘与产物校验都归它。
- **引擎内测试**。规则层测试不需要引擎；场景、输入与帧级测试要等 `ENG-6`，届时再选方案
  （GdUnit4Net 已实测可在 4.7.2 + `net10.0` 上跑引擎内测试，但本条用不上，不提前引入依赖）。
- **状态效果系统**。正典把它定为所有临时与持续效果的统一载体，是跨系统基础设施，但它的设计
  归玩法实现需求。

## `project.godot` 里不要动的两处

- `window/stretch/aspect="expand"` —— 让可视区域随宽高比伸缩。它是 `UI-2`（Steam Deck 等
  其他宽高比适配）的零成本前提，正典明确要求保持不动。
- `renderer/rendering_method="mobile"` —— 2D 项目的性能选择，**与目标平台无关**。正典特地写明
  过这一点，免得有人以为项目要上移动端（不上，目标是 Steam 的 Windows PC 端）。

界面要用锚点与容器布局，不写死绝对像素坐标 —— 同样是 `UI-2` 的零成本前提。
