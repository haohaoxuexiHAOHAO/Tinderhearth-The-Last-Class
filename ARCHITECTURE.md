# 架构

本文写**分层、边界与为什么**。产品事实在设计仓的正典，跨域取舍在设计仓 `decisions/` 的 ADR，
本文只说代码这一侧的结构。与正典冲突时以正典为准。

## 两层：规则层与引擎层

```
tests/  Tinderhearth.Rules.Tests   ← 只测规则层
   ↓
rules/  Tinderhearth.Rules         ← 判定与结算规则，不引用 Godot
   ↑
src/    Tinderhearth-The-Last-Class ← 场景、节点、输入、显示、文件 I/O
```

依赖是**单向**的：引擎层依赖规则层，规则层对引擎层一无所知。

### 为什么规则层必须是独立程序集，而不是一条约定

实测：在引擎外构造 Godot 节点（`new Godot.Node()`）会触发**进程级 Fatal error**，崩在
`Node..cctor()`，**而且 try/catch 抓不住**。也就是说一次手滑不会变成一条失败的测试，而是
带走整轮测试的全部结果，还看不出是哪一行干的。

规则层不引用 GodotSharp，编译器就不给手滑的机会 —— 这是靠构造保证，不是靠自觉。

顺带一条实测结论，避免以后有人拿它当反例：Godot 的**纯数学结构体**（`Vector2I` 之类）在引擎外
是可用的，因为它们是纯托管类型。所以「让规则层引用 GodotSharp 换取方便」技术上行得通，但那会
把 `Node` 的门一起打开，护栏就软了。**要开这个口子，先在本文写明开的是哪个口子、为什么。**

### 这条边界同时是别的东西的地基

- 规则层不碰引擎，所以它的测试不需要启动 Godot：跑得快，而且「能不能跑测试」不依赖「引擎能不能加载程序集」。
- 这条边界在**打包时**也有执行体：`tools/verify.py` 解开 `.pck` 逐条看清单，`src/` 以外的
  `.cs` 出现在包里就判失败 —— 那意味着规则层或测试工程被 Godot 扫进了发行包。
- 联机若做，规则层是唯一需要在两端得出相同结果的东西（设计仓 `canon/gameplay/玩法定位.md` 写明联机未立项）。
- mod 只能追加内容、不能改规则 —— 规则是编译进程序集的代码，不在数据文件里。

## 模块边界照着正典的系统清单来

设计仓 `canon/gameplay/玩法定位.md` 的「系统清单」是**分层的**（五个大系统各含子系统），
那份层级就是天然的模块边界。命名空间照它排：

| 正典里的大系统 | 命名空间 |
| --- | --- |
| 跨系统基础设施 | `Tinderhearth.Rules.Foundation` |
| 时间与经营系统 | `Tinderhearth.Rules.Economy` |
| 角色与成长系统 | `Tinderhearth.Rules.Progression` |
| 战斗与关卡系统 | `Tinderhearth.Rules.Combat` |
| 叙事与推进系统 | `Tinderhearth.Rules.Narrative` |

**这张表是「东西该放哪」的权威，文件系统不是。** 目录只在有代码时才建 —— 现在只有
`Foundation/` 与 `Progression/`，因为只有它们有代码。建一堆空目录是把没做的事伪装成做了的事。

### 一处照表会更差的地方：`Rules.Ui`

输入（`UI-7`）与相机（`UI-5`）在正典里都属**跨系统基础设施**，照表应进 `Rules.Foundation`，
实际都在 `Rules.Ui`。理由是依赖方向：两者的量全部从 `UiMetrics` 推导（相机的缩放与有效视野来自
逻辑分辨率与侧视 2 倍缩放，死区写成屏幕像素再除以缩放），而 `UiMetrics` 在 `Rules.Ui`。照表放
会让 `Foundation` 依赖 `Ui` —— 基础层依赖上层，方向反了。**这是本文承认的一处偏离，不是漏改。**

相机的引擎层落 `src/World/`，只有一个类：`GameCamera`（`sealed`，两种视角共用）。两条结构性
约束：派生 `Camera2D` 的类型恰好一个，且相机位置恒为整数世界像素（唯一的取整点在
`GameCamera.RoundToPixel`）。第一条由 `sealed` 让编译器守住；第二条原有静态守卫，随 `ADR-0009`
删除后只剩约定 —— 它坏了的表现是像素抖动，作者实机看得出来。

## 战斗：三个轴，但位置的所有权在纵深轴上是反的

正典把战斗关卡定为**带连续可行走纵深的横版**（设计仓 `canon/gameplay/战斗与关卡.md`），于是运动有
三个轴。它们的所有权不一样，这一处不对称值得写明，否则下一个人只会当成漏改。

| 轴 | 速度 | 位置 | 谁做碰撞 |
| --- | --- | --- | --- |
| 横向 | `MotorState.HorizontalVelocity` | 引擎（`CharacterBody2D.Position.X`） | 引擎 `MoveAndSlide` |
| 跳跃高度 | `MotorState.VerticalVelocity` | 引擎（`CharacterBody2D.Position.Y`） | 引擎 `MoveAndSlide` |
| 纵深 | `MotorState.DepthVelocity` | **规则层**（`MotorState.DepthWorldPx`） | 无碰撞体；实体阻挡靠纵深门控引擎碰撞（`GP-17`，见下） |

**为什么纵深连位置一起归规则层**：Godot 2D 只有两个轴，已经被横向与跳跃高度占满，纵深在引擎那边
没有对应的位置量可以借；纵深上仍然没有碰撞体 —— `GP-17` 的实体阻挡也不是给纵深造一个碰撞体，而是
用纵深去门控引擎那两个轴上的碰撞（见下面单列的那一节）。反过来若让引擎持
纵深位置，48px 带的钳制就只能写在引擎层、或者两处各写一份 —— 而「钳制」正是 `GP-15` 要能脱引擎单
测的东西。

**代价写在这里**：引擎层只读 `DepthWorldPx`，**绝不许自己再积分一遍纵深速度** —— 那会得到两倍位移，
而且不报错。执行体分两半：写入口由编译器守（`DepthWorldPx` 是 `private set`，引擎层唯一能调的写法
是 `MotorState.PlaceDepth`，那是摆位用的、不产生速度），逐帧步长由规则层单元测试守（每帧步进必须
恰好等于 `CombatFeel.DepthSpeedPixelsPerSecond` 换算出
的一格）。**「引擎层不许另存一份纵深」这一半**原先只能人工核（静态检查扫不出「自己又存了一个
变量」）；`ENG-15` 把消费口收成 `IDepthActor.DepthWorldPx` 一个接口之后，`GP-16` 把命中判定的两个
参数都写成那个接口类型（`Hitbox.Connects`），于是这一半在命中这条路上**由编译器守着** —— 想另存
一份得先改签名。绘制那条路仍是评审项。

## 纵深不参与引擎碰撞，所以命中判定必须自己补一次纵深筛选

上表第三行「谁做碰撞：无」有一个直接后果，值得单独写：**碰撞形状表达不了纵深**。`Hitbox` 的实时
形状查询（`PhysicsShapeQueryParameters2D`）只覆盖 Godot 2D 的两个轴，也就是横向与跳跃高度；纵深上
既没有位置量也没有碰撞体，查询压根看不见它。所以「命中要横向与纵深两个条件都成立」（正典
`canon/gameplay/战斗与关卡.md` 的「打击反馈」，**不是可调项**）只能这么落：先让查询按横向拿到候选，
**再在规则层筛一遍纵深差**（`rules/Combat/DepthOverlap.cs`）。

这条边界会长出两个下游，都记在这里以免各写一份：`GP-17` 的实体阻挡复用同一个重叠判定，但传它自己
的阈值（「打得着」的宽容量与「占同一格」的物理量不共用一个数，见下一节）；而「候选按横向算、纵深超差
的目标仍会被查出来再丢掉」那点白算的开销，量级归 `ENG-16` 的满编探针。

## 实体阻挡：纵深门控引擎碰撞，而不是给纵深造碰撞体

`GP-17` 是上面那条边界的第二个下游，它的形状与命中判定**不一样**，值得单独写一段。

命中是「查询拿候选、规则层再筛纵深」，因为命中只需要一个是非答案。阻挡不行 —— 它要的是**位置被真的
挡住**，而算这件事（贴着走、沿墙滑、脱插）是 `MoveAndSlide` 的活，规则层重写一遍就是第二份事实
（`ENG-6`）。所以阻挡走反方向：**让引擎照常做碰撞，由纵深决定哪些对参与。**

| 那一半 | 落点 | 为什么在那里 |
| --- | --- | --- |
| 该不该挡（阵营 ＋ 纵深两个条件） | `rules/Combat/DepthBlocking.cs` | 纯判定，能脱引擎单测；复用 `DepthOverlap`，不写第二份重叠判定 |
| 阈值 | `rules/Combat/CombatFeel.cs` 的 `BlockDepthThresholdWorldPx` | 手感可调量只有一处（`FR-18`）。**与命中容差是两个常量**，此刻同为 8 是巧合不是等式 |
| 立场数据 | `rules/Foundation/Actors/CombatSide.cs` | 将来的感知系统（阵营视野并集）要读同一份，不各建一套 |
| 把结论落成碰撞豁免 | `src/World/DepthBlocker.cs` | 碰引擎的那一半：增删豁免、补掩码、报覆盖量 |

**为什么是碰撞豁免而不是碰撞层**：层与掩码是按**类**开关的，表达不了「这一对挡、那一对不挡」，更表达
不了纵深。豁免是 Godot 里唯一按对生效的开关，所以纵深门控只能落在它上面。

**两条实测结论写在这里，因为它们都不报错、只体现在画面上**（2026-09-18 实测，当时用的图形探针已删）：

1. **逐帧增删豁免是稳的。** 本项目此前只在 `_Ready` 里用过一次豁免，逐帧切换没有先例，所以专门量了：
   静止 60 帧零切换，纵深跨过阈值时那一对恰好切换一次。判据 `toggle-stable-when-still` 与
   `toggle-once-per-crossing` 钉着它。
2. **阻挡是「不许走进去」，不是「把已经在里面的推出去」。** 阻挡在两具身体已经横向叠着时才生效的话，
   Godot 的脱插是**一帧到位**的：实测单帧位移 17.77px（约等于整个本体宽 18px），两具都在 `MoveAndSlide`
   的角色此后还会锁步一起漂（60 帧从 X=220 漂到 77.8）。所以 `DepthBlocker` 有一条迟滞：**已经横向重叠
   着就先不挡，等分开了再挡**，改完实测单帧位移 0.00px。

## 阻挡有两半，纵深那一半引擎帮不上忙

上一节那张表里「把结论落成碰撞豁免」只是**横向**那一半。**纵深那一半必须自己做**，理由就是上面反复
说的那条：纵深没有碰撞体，`MoveAndSlide` 物理上拦不住纵深移动。

漏了这一半的后果是作者 2026-09-18 实机撞到的：**左右走过去被挡住，改用 W／S 在纵深上走进去却直接穿
过**。于是「绕行」形同虚设 —— 换一排走到物件跟前，再横着挪进它体内就行了。这与本条最初要修的缺陷
（主角能穿过木桩）是同一类，只是接近方向不同。

所以 `DepthBlocker.Resolve` 是两段：先 `ConstrainDepths`（把在纵深上挤进来的一方夹在阈值边界上，算法
在 `DepthBlocking.ClampDepthOutOf`），再 `UpdateExemptions`（横向那一半的豁免）。三条约束写明：

- **只对横向重叠的对生效。** 正好贴住时纵深必须自由，否则玩家走到物件跟前就再也挪不动纵深 —— 而那
  恰恰是「绕行」要用的那一步。判据 `go-around-path-open` 是这一条的正面证明：光有「挤不进去」会让
  「横向重叠就锁死纵深」那种过紧实现也全绿。
- **只拦往里、不拦往外。** 否则会把已经在里面的角色永久关住；那个状态正常操作到不了，但摆位、出场与
  将来的击退都可能造出来，而把玩家关死比让他走出来更糟。
- **夹的是边界，不是「上一帧位置加位移」**，所以某一帧位移再大也不会穿到另一侧。

**代价写明**：夹逼滞后一帧（与豁免同一个口径），所以最多插进 1px —— 纵深每帧最多走 1 世界像素
（`DepthSpeedPixelsPerSecond`＝60，60Hz）。判据容的就是这一帧步长。

**阻挡阈值 ≤ 命中容差**是一条不变量，由单测钉着：反过来会出现「被挡住却打不着」，玩家贴着一个够不到
的东西、读不出原因。两个数都归 `GP-6` 各自调，正因为能各自动才需要这条判据。

**阻挡由场景显式装配，不是所有实体自动带上**（木桩那条 `Layer=4`／`Mask=1` 的单向关系不受影响）。
目前只有训练房装。谁该挡谁是场景的事，所以这件事按 `ADR-0009` 归作者在编辑器里配。

纵深的坐标口径（0 最靠后、48 最靠前、与屏幕向下同向）与它的理由在 `rules/Combat/DepthBand.cs`，
不在本文抄第二份。

## 界面：量在规则层，节点在引擎层

`UI-8` 的关卡 HUD 把这条边界推到了极致，值得单独写一段，因为后续每个界面都照它做。

| 东西 | 落点 | 为什么在那里 |
| --- | --- | --- |
| 排版量（块尺寸、贴哪个角、占屏比例、可读区） | `rules/Ui/HudLayout.cs` | 它们之间有**可以判死的关系**（块高是行高的整数倍、块不许压角色可读区、四块不许两两重叠），放规则层就有单元测试盯着 |
| 显示的量（HP／技能冷却／目标进度／队友血量） | 调用方传进来的 `rules/Ui/HudViewModel.cs` | 玩法数值归 `design/数值模型.md`，`UI-1` 不读也不搬 |
| 占位配色 | `rules/Ui/HudPalette.cs` | `DOC-2` 定稿时改一个文件，不必翻界面代码。颜色类型**没有 alpha 字段** —— 半透明遮罩画上去就是屏幕上的插值像素 |
| 字体的期望渲染参数 | `rules/Ui/PixelFont.cs` | 设置在 `.ttf.import`、期望在这里、实际值由引擎报，三处独立才互为量具 |
| 节点、锚点、素材加载 | `src/UI/LevelHud.cs` | 碰引擎的那一半 |
| 演示数据 | `src/World/HudDemoModel.cs` | 与脚手架同性质。**唯一有数字的地方必须在界面之外**，那个分工正是「界面里没有写死的数」扫得干净的前提 |

结果是 `LevelHud.cs` 里**除 0 与 1 之外一个数字字面量都没有**。这条原先有静态扫描守着，守卫随
`ADR-0009` 删除，现在是一句约定（见 `CONVENTIONS.md`）。

**两个 Godot 的坑写在这里，因为它们会让「界面看起来少了几块」而不报错：**

1. **对已经在树里的 Control 调 `SetAnchorsPreset` 不会让它铺满。** 2026-08-31 实测：引擎会把
   偏移改写成负的父尺寸以保住当前那个 0×0 矩形 —— 锚点确实变成了 0,0,1,1，尺寸还是 0×0，
   于是贴下边与贴右边的子节点落到负坐标、画在屏幕外。那个 `keepOffsets` 参数的含义与名字给人的
   印象相反。**要铺满就用 `SetAnchorsAndOffsetsPreset`**，它与调用顺序无关。`UiRoot.Register`
   原先写在 `AddChild` 之前碰巧躲过了，已改成不依赖顺序。
2. **`set_offsets_preset` 按内容最小尺寸算偏移，实际尺寸却按「内容最小与 `CustomMinimumSize`
   取大」定。** 所以块的尺寸算式（`HudLayout`）与容器的实际排法（间距、内边距）必须逐项对上，
   差 4px 就会让整块偏出安全边距。规则层预测有单元测试盯着；「节点的实际矩形是否等于那个预测」
   原先由启动探针核，探针随 `ADR-0009` 删除后归作者实机看。

子系统清单**不在本文重抄一遍**：正典是它的唯一权威源，抄一份必然漂移。要知道某个子系统属于哪个
大系统、第一版做不做，去查正典那三份分册。

## 内容与 mod 的加载

**基础内容 `res://data`，mod `user://mods/<名字>/`，后者叠在前者之上。**

先纠正一条本文自己曾经写错的说法：**「导出后 `res://` 只读」是错的。** 2026-08-30 用导出产物实测
（Windows、非嵌入 pck）：往 `res://` 写文件**会成功**，句柄非 null、错误码 `Ok`，文件落在**可执行
文件同级目录**。只有已经打进 `.pck` 的条目才真正改不了，`res://` 这个前缀本身是可写的。

所以 mod 放 `user://` 的理由不是「res:// 写不了」，而是这三条：

1. **exe 同级目录不按用户区分。** 多用户共享一份安装时，一个人装的 mod 会影响所有人。
2. **真实安装位置常常写不了或会被抹掉。** `C:\Program Files` 下要提权；Steam 库目录即使可写，
   「验证文件完整性」也会清掉不在清单里的文件 —— 玩家的 mod 会莫名消失。
3. **Steam 云存档要求一个可预测的按用户位置**，`user://` 正是它（`ENG-8`）。

已实测：`user://` 可创建可读写；放一个探针 mod 进去后它被发现、内容被叠加、基础内容被覆盖，
验证完已删除。

### 两种覆盖语义，别统一成一种

| 内容形状 | 语义 | 例子 |
| --- | --- | --- |
| 一个文件一个实体 | **整文件覆盖**，后来的来源整份替换 | 角色、宝物、配方、敌人 |
| 一个文件很多条目 | **逐条目合并**，只覆盖同键条目 | 文本表 |

这个区分是实测撞出来的，不是设计时想到的：最初文本也走整文件覆盖，结果一个只提供三条文本的 mod
把基础文本从 5 条顶成 3 条，`boot.title` 直接消失 —— 界面上会冒出一片缺文本占位。改成逐键合并后
同一场景是 6 条（基础 5 条 + mod 新增 1 条，2 条被覆盖）。

对应代码：`ContentCatalog.Resolve` 是整文件覆盖，`ContentCatalog.ResolveAll` 不去重、由调用方
决定合并方式；文本走 `TextCatalog.Merge`。两条语义都有测试盯着。

### 每条内容记得来自哪个来源

`ContentEntry` 带 `SourceName`。这不是为了诊断好看：待办 `ENG-7` 要求卸载 mod 后能列出「因缺少 mod
而不可用」的角色与物品，那件事只能靠来源信息做，事后补要翻遍整条加载路径。

## `ENG-5` 的四条零成本预留在哪

这四条的共同点是**失效方式都静默**：图省事写死一个数字或一句中文，代码照样跑、游戏照样玩，
等到做 mod 或联机才发现要翻遍代码。所以每条都有测试盯着（`tests/Foundation/Eng5ReservationTests.cs`）。

| 预留 | 落点 |
| --- | --- |
| 角色定义外置成数据 | `Progression/CharacterDefinition.cs` + `data/characters/*.json` |
| 名册容量从配置读 | `Foundation/Config/GameConfig.cs` + `data/config/game.json` |
| 本地玩家／AI／远程玩家统一成控制器接口 | `Foundation/Actors/ActorControl.cs` |
| 全部文本外置 | `Foundation/Text/TextCatalog.cs` + `data/text/*.json` |

关于控制器接口，有一条容易被写坏、所以单独写明：**「玩家」不是「主角」的同义词。** 主角只是恰好
通常由 `LocalPlayer` 驱动的一个角色。谁驱动谁由 `ActorControllerRegistry` 决定，任何
「这个角色是不是主角」的判断都不得用来决定它由谁驱动，反之亦然。有一条测试直接把主角换成 AI 驱动
来盯这件事。

## 刻意还不存在的东西

写下来是为了让「还没做」和「忘了做」区分得开：

- **任何玩法数值。** 属性公式、成长曲线、判定公式、价格、消耗量与时长的设计在设计仓
  `design/数值模型.md`，但**代码这一侧还没有消费它们的系统**。
  代码与数据里都**刻意没有**这类数字 —— 现在写进去就是把猜出来的数伪装成配置。
- **`UI-1` 的界面部分只剩世界空间那一半。** 逻辑分辨率（`UI-3`）、界面层级与导航栈（`UI-6`）、
  输入映射（`UI-7`）、相机五项行为（`UI-5`）、像素字体与关卡 HUD 的屏幕空间部分（`UI-8`）已落地，
  代码在 `rules/Ui/`、`src/UI/` 与 `src/World/`。**世界空间 UI 还没有**（`UI-9`：读条、精英血条、
  伤害数字 —— 正典要求它们画在执行者身上，所以挂 `UiLayer.WorldSpace` 而不是 `Hud`）。
- **相机只有行为，没有场景。** `UI-5` 交出的是跟随、钳制、震动、演出接管与建造滚动推镜这几项
  能力加一套判据；基地场景、建造界面与演出系统本身都不在（分别归经营侧、建造实现与叙事侧）。
  演出那一项只做「能接管、能归还、归还后行为与接管前一致」这个接缝。
- **引擎内测试**。规则层测试不需要引擎；场景、输入与帧级测试要等 `ENG-6`，届时再选方案
  （GdUnit4Net 已实测可在 4.7.2 + `net10.0` 上跑引擎内测试，但本条用不上，不提前引入依赖）。
- **状态效果系统的完整扩展**。A1 的硬直与无敌已在 `rules/Combat/StatusEffects.cs` 共用最小载体，
  `MotorState.Statuses` 持有角色状态并负责每个非顿帧逻辑帧推进一次（`GP-11`）。其他异常状态、
  解除手段与跨系统效果仍归后续玩法需求，不另建平行载体。

## 纹理导入的三项覆盖，为什么是这三项

`project.godot` 的 `[importer_defaults]` 覆盖了三个纹理导入参数。**理由写在这里而不是写成那份
文件里的注释** —— 2026-08-31 实测：在编辑器里打开一次工程，Godot 会重写 `project.godot`，
**把注释整段删掉**并重排键的顺序，不报错也不提示。把「为什么」放在会被工具重写的文件里，等于
让它自己消失。

| 参数 | 值 | 为什么 |
| --- | --- | --- |
| `detect_3d/compress_to` | 0 | 引擎默认是 1，也就是「发现这张图用在 3D 就重新导入成 VRAM 压缩」。本作是纯 2D，理论上不会触发，但一旦触发就是像素图被有损压缩 —— 画面只是「有点花」，不报错。关掉它比事后排查便宜得多 |
| `compress/mode` | 0 | 无损。与引擎默认一致，写出来是为了让它成为一条能核的判据，而不是依赖「默认值不会变」 |
| `mipmaps/generate` | false | 缩小级别对像素风没有意义。同样是为了可核 |

**注意项目级设置只影响「将来新导入」的文件**，已经导入过的素材各自把这三项固化在自己的 `.import`
里。原先有守卫逐个读 `.import` 核这三项，它随 `ADR-0009` 删除 —— **导入配置现在归作者在 Godot 里
管**（这正是那份分工的核心）。这三项配错的表现是像素图发花或被压糊，实机一眼能看出来。

## `project.godot` 里不要动的两处

- `window/stretch/aspect="expand"` —— 让可视区域随宽高比伸缩。它是 `UI-2`（Steam Deck 等
  其他宽高比适配）的零成本前提，正典明确要求保持不动。
- `renderer/rendering_method="mobile"` —— 2D 项目的性能选择，**与目标平台无关**。正典特地写明
  过这一点，免得有人以为项目要上移动端（不上，目标是 Steam 的 Windows PC 端）。

界面要用锚点与容器布局，不写死绝对像素坐标 —— 同样是 `UI-2` 的零成本前提。
